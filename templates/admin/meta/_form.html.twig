{{ form_start(form) }}

<div class="card-body">
    <div class="form-group">
        <label>Meta title:</label>
        {{ form_widget(form.title, {'attr': {'class': 'form-control form-control-solid'}}) }}
        <span class="form-text text-muted">{{ form_errors(form.title) }}</span>
    </div>
    <div class="form-group">
        <label>Meta description:</label>
        {{ form_widget(form.description, {'attr': {'class': 'form-control form-control-solid'}}) }}
        <span class="form-text text-muted">{{ form_errors(form.description) }}</span>
    </div>
    <div class="form-group">
        <label>Entity:</label>
        {{ form_widget(form.entity, {'attr': {'class': 'form-control form-control-solid'}}) }}
        <span class="form-text text-muted">{{ form_errors(form.entity) }}</span>
    </div>
    <div class="form-group">
        <label>Id Entity:</label>
        {{ form_widget(form.idEntity, {'attr': {'class': 'form-control form-control-solid'}}) }}
        <span class="form-text text-muted">{{ form_errors(form.idEntity) }}</span>
    </div>
</div>
<div class="card-footer">
    <div class="row">
        <div class="col-lg-6">
            <button type="submit" class="btn btn-primary mr-2">Enregistrer</button>
            <button type="reset" class="btn btn-secondary">Annuler</button>
        </div>
        <div class="col-lg-6 text-lg-right">
            <a href="{{ path('app_meta_index') }}">back to list</a>
        </div>
    </div>
</div>
{{ form_end(form) }}
