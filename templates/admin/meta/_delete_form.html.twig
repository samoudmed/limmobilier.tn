<form method="post" action="{{ path('app_meta_delete', {'id': metum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ metum.id) }}">
    <button class="btn btn-light-danger font-weight-bold">Supprimer</button>
</form>
