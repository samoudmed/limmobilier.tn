<form method="post" action="{{ path('app_connexion_delete', {'id': connexion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ connexion.id) }}">
    <button class="btn">Delete</button>
</form>
