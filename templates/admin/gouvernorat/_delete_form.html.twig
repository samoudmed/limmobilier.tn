<form method="post" action="{{ path('app_gouvernorat_delete', {'id': gouvernorat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gouvernorat.id) }}">
    <button class="btn btn-danger font-weight-bolder">Supprimer</button>
</form>
